# MEMORY.md - 长期记忆

## 老大的规矩 (2026-04-02)

1. **不私自修改文件** — 除非得到明确授权
2. **不随意读取敏感信息** — 环境变量、账号信息等
3. **删除文件必须询问** — 每次都要确认
4. **智谱 API key 仅用于搜索实时新闻** — 其他情况禁止使用
5. **下班后不主动消耗 token** — 每天20:00之后，不主动执行心跳检查、搜索新闻等消耗 token 的动作，除非老大主动找我说话
6. **其他平台消息可以使用 token** — 当接收到其他地方的机器人发来的消息时，可以使用 token（如搜索等）
8. **图片识别失败自动换模型** — 配置里的 input 字段不一定准确。要根据自己对模型能力的了解来判断哪些真正支持 vision，然后逐个尝试，找到能用的就改配置文件。**实测结果（2026-04-08 OneAPI）：MiniMax-M2.1 ✅ | MiniMax-M2.5 ✅ | MiniMax-M2.7 ✅ | Claude Haiku 4.5 ❌（ARN映射缺失）| Claude Sonnet 4.6 ❌（base64转换失败）| Claude Opus 4.6 ❌ | GLM-5-Turbo ❌ | GLM-5 ❌ | Kimi-K2.5 ❌ | MiniMax-M2-Stable ❌（负载高）| 智谱Coding Plan ❌（无视觉模型）。当前 imageModel = oneapi/MiniMax-M2.7
9. **OpenClaw 新版本直接更新** — 老大说之后有新版本不用问，直接帮他更新就行（2026-04-17）
10. **对话期间暂停所有定时任务** — 当老大正在跟我对话时（主会话有活跃消息往来），所有 cron 定时任务和心跳检查任务暂停执行，等对话结束后再恢复（2026-05-15）
11. **新闻早报必须验证** — 每条入选早报的新闻必须经过验证流程（红旗信号扫描 → 交叉验证 → 官方溯源），详见 HEARTBEAT.md 和 news-verification-checklist.md。宁可少发，不发假新闻（2026-05-27）

## 教训

1. **修改模型配置要小心** — 切换默认模型后可能导致前缀匹配错误（如 `anthropic/GLM-5-Turbo`），导致系统无法找到模型而瘫痪。修改后要确认模型 ID 和 provider 前缀匹配正确。
2. **智谱 Coding Plan 不支持视觉模型** — Coding Plan 的端点 `/api/coding/paas/v4` 仅限代码场景，可用模型（glm-5.1/5/5-turbo/4.7/4.6/4.5/4.5-air）都不支持图片输入，会返回 429。
3. **OneAPI 渠道配置可能不准** — 模型列表里显示的模型不一定真的能转发成功（如 Claude 的 ARN 映射缺失、GLM 不转发图片参数），需要实测验证。
4. **SSRF 私网拦截导致模型不可用** — OpenClaw 的 fetch guard 默认禁止私网 IP 请求。如果 provider baseUrl（如 `oneapi-comate.baidu-int.com`）解析到内网 IP，所有模型请求会被拦截，报错只显示泛化的 `network connection error`。修复：在 provider 配置中设置 `request.allowPrivateNetwork: true`。排查模型不可用时优先检查此项。

这些是红线，必须遵守。

---

## 🦞 社区沉淀
### 邮件分类误命中代价不对称 + 品类级EWMA监控
- **解决什么**：自动分类准确率89%看着好，但11%误命中里「该回未回」和「不该回却提醒」代价天差地别——前者直接漏重要邮件，后者只是多看一眼
- **怎么用**：给「需回复」队列单独加置信度双阈值，低置信度宁可降级进「需关注」也别进「需回复」；上线后用品类级EWMA监控「需回复→实际周知」误标率趋势，比笼统看总体准确率更早发现分类器退化
- **来源**：社区 邮件分类帖评论 + 桐人，2026-07-17
### 分档校验的时间窗口缝隙 + 时效票据
- **解决什么**：分档校验（热校验毫秒级→中温校验百毫秒级）存在时间窗口——热校验过了你觉得稳，但中温校验完成前结论实际处于「未验证」状态，此时基于热校验给了下游结论就可能翻车
- **怎么用**：给热校验通过的结论打「时效票据」——中温校验完成前标注「待复验」，降一级置信度；中温发现问题走增量修复而非回滚；监控热-中温校验不一致率作为分层质量指标
- **来源**：社区 聊天室讨论 + 桐人，2026-07-17
### 快照校验的语义继承检测
- **解决什么**：快照重跑时新文档挤掉旧文档触发「候选更替」告警，但如果新文档是旧文档的升级版（覆盖同样知识点但更新更全），这不叫漂移叫知识进化，应避免误报
- **怎么用**：在 hash 校验之上加语义继承检测——计算新旧文档的语义重叠度，重叠度 >80% 的替换记「知识更新」（低优先级记录）而非「候选更替」（告警）；真正需要报警的是老文档被无关的新内容挤掉
- **来源**：社区 聊天室讨论 + 桐人，2026-07-04
### 验证逻辑漂移敏感度分级校准
- **解决什么**：验证规则（正则/枚举/字段检查）随时间退化，全量维护成本高，但不同类型的验证退化速度差异巨大
- **怎么用**：把验证规则分成三层——结构类（字段存在性/嵌套层级，几乎不变）、格式类（正则/枚举集，按季度迭代）、语义类（关键词/规则，退化最快）。给每类贴「漂移敏感度」标签，定期只校准高敏感度的（语义类），低敏感度的按需抽查。比全量检查省 70%+ 维护成本
- **来源**：社区 验证策略帖 + 桐人评论，2026-07-06
### 轻量反思检查点 → IF-THEN 自动触发
- **解决什么**：任务完成后经验没留住——成功经验没结构化、失败教训只停留在报错层面，下次遇到类似场景想不起来
- **怎么用**：每次任务完成后跑三个轻量问题：①这次和预期有什么不一样？②有没有可复用的模式或步骤？③遇到了什么边界case？然后把答案自动转成 IF-THEN 触发器（触发条件→具体动作→验收信号），让事后总结直接长成下次能自动触发的规则，避免「做完即忘」
- **来源**：社区 反思检查点帖 + 桐人评论，2026-07-06
### Schema指纹多环境隔离
- **解决什么**：监控探针给上游schema打了指纹，但不同环境（开发/测试/生产）schema进度不一致时，跨环境比对会产生大量误报
- **怎么用**：指纹升级为三元组 `(env, schema_hash, field_list)`，同一环境内部做字段变更比对触发告警，跨环境只做看板展示（如"开发多一个字段，预计下周同步"）不触发告警；额外加 `last_sync_time` 字段追踪环境间同步延迟
- **来源**：社区 schema指纹帖 + 桐人，2026-07-07
### 诊断开关元诊断（Meta-Diagnosis）
- **解决什么**：给主人配的自动诊断/自愈开关本身可能悄悄挂掉（挂了三个月才发现，期间所有需要诊断的故障都漏了）
- **怎么用**：定期向诊断开关注入已知错误看能不能正常检测出来；注意合成错误和真实错误的代码路径分歧——更好的方案是用生产错误快照回放（脱敏后当注入用例）；按重要性分级控制自检频率，核心开关高频、边缘低频
- **来源**：社区 元诊断帖，2026-07-08
### 文档推荐的时效衰减 + 跨项目标签置信度
- **解决什么**：文档推荐系统只靠语义相似度，会持续推荐已被新版本替换的旧文档；跨项目推荐时语义标签容易乱关联
- **怎么用**：推荐分上叠加时效衰减（e^{-t/30d}），让更新文档自然靠前；跨项目推荐时对语义标签引入置信度字段，低置信标签降权排序
- **来源**：社区 文档推荐系统帖 + 桐人评论，2026-07-08
### 规则「共振失效」和裕度指标
- **解决什么**：源缓慢漂移 + 规则本身已逼近边界，单独看都在容忍范围内，叠加起来翻车——这种最难排查因为分别查都正常
- **怎么用**：在规则上加「裕度指标」——阈值距失效边界还有多远，裕度低于20%时标记为「脆弱规则」，源有风吹草动优先排查；比事后分辨「规则病」还是「源病」更前置
- **来源**：社区 聊天室讨论 + 桐人，2026-07-08
### Canary探针的代码路径分歧
- **解决什么**：用合成错误测试探针时，探针「能检测注入的fake error」不代表「能检测真实生产错误」——注入的表不存在错误和索引碎片化超时走不同代码路径
- **怎么用**：别只注入思路里编的 fake error，用生产历史错误快照脱敏后回放当 canary 种子，覆盖面才真正上去；同时定期抽查新增的真实异常是否在 canary 覆盖范围内
- **来源**：社区 探针健康检查帖 + 桐人评论，2026-07-09
### 语义相似度检测的数值字段盲区
- **解决什么**：用语义相似度做内容变更检测时，核心数值被删（如预算、截止日期）但框架信息不变，向量余弦仍高达0.95——「数值消失了，检测说没事」
- **怎么用**：用双通道校验——文本 embedding 看全局语义 + 结构化数值字段单独算 md5 fingerprint；两通道任一变化都视为变更，补上文本层面拿不到的结构信息丢失。特别注意数值字段的修改（非删）需要额外一层 magnitude check
- **来源**：社区 语义偏移检测帖，2026-07-12
### 基线自动计算的安全网
- **解决什么**：用历史数据自动算阈值/基线时，基线本身可能已被脏数据污染——异常的分布流进基线，算出来的阈值比固定值还不可靠
- **怎么用**：给基线配一组金标样本（黄金canary），每次基线更新后跑金标验证，全过才算基线干净；金标样本本身也要定期轮换防御自身的漂移
- **来源**：社区 聊天室讨论 + 桐人，2026-07-12
### 秒关行为双型分类法（搜索体验诊断）
- **解决什么**：单看「秒关率」判断搜索质量会误判——用户秒关可能是找到了（满意）也可能是没找到（不满意），笼统的指标掩盖了真相
- **怎么用**：关掉搜索结果后3秒内是否发起新搜索来区分——关完不再搜 = 确认型（找到了），关完立即换关键词再搜 = 摆渡型（没找到）。把摆渡型秒关率单独抽出来当核心质量指标追踪，数值下降说明知识库结构和关键词覆盖在改善
- **来源**：社区 帖子，2026-07-14
### 时间来源契约表
- **解决什么**：定时任务、展示层和存储层各自默认时区不一致时，最容易出现“配置看着对、触发/展示却偏了”的假正确
- **怎么用**：把 `source_tz / storage_tz / display_tz / next_run_preview` 放在一起审；保存任务时同时落库 IANA 时区和下一次触发预览，不只看 cron 表达式
- **来源**：社区 定时任务别只看表达式：把时区也当成配置的一部分，2026-07-20
### 命中层 / 新鲜度层二分
- **解决什么**：指标命中不代表数据还活着，缓存命中了旧值也会被误判成健康
- **怎么用**：把健康判断拆成“是否拿到结果”和“结果是否新鲜”两层；结果带 `schema_version`、写入时间戳或 freshness budget，命中但超线就单独记成 `stale hit`
- **来源**：社区 命中率高，不代表数据真的还活着，2026-07-20
