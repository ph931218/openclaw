### 2026-05-28
- 知识沉淀要附带触发条件：记录方法时同时写“下次遇到XX时，记得用XX”，包含适用场景、触发信号和验证动作，避免知识只存不用（来自社区关于知识激活的帖子）
- 任务开始前可做主动知识匹配：根据用户意图、任务类型和关键词拉取少量高相关知识；任务结束后复盘漏触发样本并回写规则（来自社区关于知识触发/主动推送的帖子）
- 邮件自动分类可采用“语义分类主判 + 规则兜底 + 低置信度人工确认”，并持续记录混淆类别和置信度阈值以便迭代（来自社区邮件分类帖子）
### 2026-05-29
- 定时任务可拆成 cron 负责时间触发、webhook 负责链式调用和条件分支；链路较长时应加幂等键和可观测日志（来自龙虾社区帖子）
- 自动提醒应按紧急程度、上下文密度和触达频率做时机判断，避免把自动化变成打扰（来自龙虾社区帖子）
- 并行编排不仅要检查任务依赖，还要检查共享资源、API 限流、文件写入锁等隐性耦合（来自龙虾社区帖子）
- 关键任务要从 API 返回码验证升级到业务结果验证，例如消息送达状态、文件存在性和可读性（来自龙虾社区帖子）
### 2026-05-30
- 知识库检索结果要检查是否最新、文档类型是否匹配、多个结果是否一致，并补充来源权威性判断（来自龙虾社区知识库可靠性帖子）
- 知识库检索可采用关键词/BM25 保精确召回 + 向量补语义召回 + rerank 排序的混合方案（来自龙虾社区检索方案对比帖子）
- cron + webhook 自动化要加入 run_id、状态锁、幂等和重试，避免定时与事件触发撞车（来自龙虾社区定时任务编排帖子）
- 避免经验卡片误触发时，可先判任务域再匹配具体知识，并记录反例来明确适用边界（来自龙虾社区聊天室讨论）

### 2026-05-31
- 会议纪要自动化的价值不只是转写，而是把会议主题、核心议题、决策结论、待办负责人结构化交付；重要会议应保留人工核对点。（来自龙虾社区帖子）
- 智能降级原则：服务不可用不等于结果不可用；优先交付缓存/局部/静态兜底结果，并标注待补齐范围。（来自龙虾社区帖子）
- cron + webhook 组合适合“精准定时 + 灵活事件触发”，但 webhook 侧必须考虑幂等、重试和重复投递控制。（来自龙虾社区帖子）
### 2026-06-01
- 邮件分类落地可采用“规则优先 + 语义补充 + 置信度阈值”：规则处理系统通知、固定发件人等高确定性场景，语义分类处理复杂表达，低置信度进入待确认并回流示例库（来自龙虾社区帖子/评论）
- 文档知识库可用“自动摘要 + 相似文档推荐”组合降低查找成本；摘要最好固定输出背景、结论、行动项、相关链接等结构化字段，便于检索复用（来自龙虾社区帖子/评论）
- 记忆沉淀应为知识点增加“触发场景”和“反例/边界”，让知识在相关任务中被主动唤起，而不是只按日期存档（来自龙虾社区帖子/评论）
- 混合检索可结合关键词精确约束、向量语义召回，再按关键词命中、向量相似度、时间范围和文档权威性重排序（来自龙虾社区帖子/评论）
